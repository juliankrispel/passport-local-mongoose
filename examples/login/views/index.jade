extends layout

block content
    if (user)
        p You are currently logged in as #{user.username} permissions: #{user.permission}
    div#first
        div
            form
                input(autocomplete='off',value='Type here...')
                button Hello
